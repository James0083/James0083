# 구글드라이브 "경제시황분석" 뷰어 웹페이지 — 구현 계획서

## 0. 결론 먼저: 실행 가능성

**어렵지 않습니다.** 백엔드 서버 없이 GitHub Pages(순수 정적 호스팅)만으로 구현할 수 있습니다.
다만 "구글 드라이브의 비공개 파일을 브라우저가 어떻게 읽어오는가"에 대한 인증 방식만 하나 결정하면 됩니다. 아래 1번 항목에서 실제 폴더 상태를 조사한 결과와, 2번 항목에서 그 결정지점을 정리했습니다.

---

## 1. 현재 구글 드라이브 상태 (직접 조회 결과)

| 항목 | 내용 |
|---|---|
| 폴더명 | `경제시황분석` |
| 폴더 ID | `1KIxaeD2vde9-KgsOUefKzqUVGhWyLfze` |
| 소유자 | jaewon000830@gmail.com (본인) |
| 공유 상태 | **비공개** — 권한 목록에 소유자 본인만 존재 ("링크가 있는 모든 사용자" 공유 아님) |
| 파일 개수 | 7개 (모두 `.md`) |
| 파일명 규칙 | `briefings_YYYY-MM-DD.md` (예: `briefings_2026-09-07.md`) — 날짜순 정렬이 파일명만으로 가능 |
| 파일 구조 | YAML 프런트매터(`title`, `date`) + 마크다운 본문(표, 굵게, 인용구 등 GFM 문법 다수 사용) |
| ⚠️ 발견된 이슈 | 일부 파일 본문에 이모지가 `ð` 같은 깨진 문자로 저장되어 있음(인코딩 손상 추정). 렌더링 시 그대로 노출되지 않도록 확인 필요 |

폴더가 **비공개**이고 내용이 개인 투자 코멘트라는 점이 이후 인증 방식 결정에 가장 중요한 변수입니다.

---

## 2. 핵심 과제: 백엔드 없이 비공개 드라이브 파일을 브라우저에서 읽기

GitHub Pages는 정적 파일만 서빙하므로, "서버가 대신 구글에 로그인해서 파일을 가져와주는" 방식은 쓸 수 없습니다. 대신 **브라우저가 직접** Google Drive API를 호출해야 합니다. 3가지 방법을 검토했습니다.

### 방법 A (권장) — 클라이언트 사이드 OAuth (Google Identity Services)
- 방문자가 웹페이지에서 "Google로 로그인" 버튼을 눌러 **본인 구글 계정**으로 인증
- 브라우저가 발급받은 access token으로 Drive API(`files.list`, `files.get?alt=media`)를 직접 호출
- **폴더를 지금처럼 비공개로 유지**할 수 있음 — 로그인한 계정에 접근 권한이 있을 때만 데이터가 응답됨
- 리포지토리(`james0083/james0083`)나 웹페이지 코드 자체는 공개되어도, 실제 브리핑 "내용"은 구글 계정 권한으로 보호됨
- 단점: 방문할 때마다 로그인 필요, Google Cloud Console에서 OAuth 클라이언트 ID를 한 번 발급해야 함

### 방법 B — 폴더 공개 전환 + API 키만 사용
- 폴더를 "링크가 있는 모든 사용자(뷰어)"로 전환하고, API 키만으로 조회
- 로그인 절차 없이 바로 목록이 보여 구현이 가장 단순
- **단점**: 개인 투자 코멘트가 사실상 인터넷에 전면 공개됨(현재 비공개 상태와 정반대). API 키도 클라이언트 JS에 노출되므로 HTTP 리퍼러 제한이 필수
- 콘텐츠 성격상 권장하지 않음

### 방법 C — GitHub Actions로 빌드타임 동기화
- GitHub Actions가 주기적으로(예: 매일) 서비스 계정/리프레시 토큰으로 Drive에서 파일을 가져와 리포지토리에 커밋 → Pages가 정적 파일로 서빙
- 장점: 클라이언트에 API 키/토큰이 전혀 노출되지 않음, 페이지 로딩이 빠름
- 단점: 리포지토리가 public이면 결국 파일 내용이 그대로 공개됨(방법 B와 같은 문제). 실시간이 아니라 동기화 주기만큼 지연 발생. 워크플로우/Secrets 설정이 별도로 필요

### 권장안
현재 폴더가 비공개이고 개인 투자 코멘트를 담고 있으므로 **방법 A(OAuth 로그인)**를 권장합니다. 방문 시 로그인 한 번 하는 불편함은 있지만, 코드는 공개 저장소에 두면서도 콘텐츠 접근은 구글 계정 권한으로 통제할 수 있는 유일한 방법입니다.

---

## 3. 아키텍처 개요

```
GitHub Pages (정적 호스팅)
└── index.html            좌측 파일 목록 + 우측 본문 뷰어 레이아웃
└── app.js
      ├─ Google Identity Services 로그인 버튼 렌더 → access token 획득
      ├─ Drive files.list (q: parentId=폴더ID and mimeType='text/markdown' or 'text/plain')
      │     → 파일명에서 날짜 파싱해 최신순 정렬 → 목록 렌더
      ├─ 목록 항목 클릭 시 files.get?alt=media 로 원문 텍스트 로드
      ├─ marked.js 로 마크다운 → HTML 변환 (GFM 표 지원 옵션 켜기)
      ├─ DOMPurify 로 XSS 방지용 sanitize
      └─ 프런트매터(title/date) 파싱해 헤더에 표시, 본문 렌더링
└── style.css             카드형 목록 + 읽기 좋은 본문 타이포그래피(가독성 위주)
```

- 별도 빌드 도구 없이 순수 HTML/CSS/JS로 시작 (필요시 Vite 등으로 확장 가능하나 이 규모에선 과함)
- 외부 라이브러리는 CDN으로 로드: `marked`(마크다운 파서), `dompurify`(살균)

---

## 4. 사전 준비 (Google Cloud Console, 1회성 설정)

1. Google Cloud 프로젝트 생성
2. **Google Drive API** 활성화
3. OAuth 동의 화면 구성
   - 개인용이라면 "테스트" 상태로도 충분 (본인 이메일을 테스트 사용자로 등록)
4. OAuth 클라이언트 ID(유형: **웹 애플리케이션**) 발급
   - "승인된 자바스크립트 원본"에 GitHub Pages 도메인 등록 (예: `https://james0083.github.io`)
5. 스코프는 읽기 전용인 `https://www.googleapis.com/auth/drive.readonly` 만 요청

---

## 5. 배포 관련 결정 필요 사항

현재 리포지토리는 `james0083/james0083` — GitHub 프로필 README용 특수 저장소입니다(리포지토리명이 계정명과 동일).

- **이 저장소에 그대로 구현 시**: GitHub Pages 주소는 `https://james0083.github.io/James0083/` 형태의 프로젝트 페이지가 됩니다. 프로필 README와 웹앱 코드가 한 저장소에 섞이는 점은 감안 필요.
- **대안**: 별도 저장소(예: `james0083.github.io`로 이름 지으면 루트 도메인 `https://james0083.github.io/`) 를 새로 만들어 그곳에 구현.

지금 지정된 브랜치(`claude/google-drive-economics-webpage-5zd93u`)가 이 저장소 기준이므로, 특별한 말씀이 없다면 **이 저장소 안에 구현하고 프로젝트 페이지로 배포**하는 것을 기본안으로 진행하겠습니다.

---

## 6. 구현 단계 (Phase)

| Phase | 내용 |
|---|---|
| 1 | 정적 사이트 뼈대 생성 + GitHub Pages 배포 워크플로우(GitHub Actions `actions/deploy-pages` 또는 저장소 설정에서 브랜치 지정) |
| 2 | Google 로그인 연동 (Identity Services 토큰 클라이언트) 붙이기 |
| 3 | 폴더 내 파일 목록 조회 → 파일명 날짜 파싱 → 최신순 목록 렌더링 |
| 4 | 목록 클릭 시 `.md` 원문을 가져와 마크다운 렌더링(표/굵게/인용구 스타일 포함), 프런트매터 제목·날짜 표시 |
| 5 | 에러/로딩 처리 — 토큰 만료, 권한 없음, 네트워크 실패 등 사용자 친화적 안내 + 반응형 레이아웃 |
| 6 (선택) | `sessionStorage` 캐싱으로 API 호출 최소화, 다크모드, 폴더 내 하위 폴더 지원 등 확장 |

---

## 7. 리스크 및 유의사항

- **Drive API 쿼터**: 무료 한도로 충분 (현재 파일 7개, 개인 사용 수준의 트래픽)
- **OAuth "테스트" 모드 제약**: 앱을 게시(publish)하지 않고 테스트 상태로 두면, 등록된 테스트 사용자 계정만 로그인 가능하고 refresh token이 아닌 access token은 유효기간이 짧습니다(재로그인 필요). 본인만 사용할 경우 테스트 모드로 충분하지만, "로그인 버튼을 누를 때마다 매번 로그인" 정도의 불편은 감수해야 합니다.
- **인코딩 손상 이슈**: 조사 중 발견된 이모지 깨짐(`ð` 등)은 원본 파일 자체의 문제로 보입니다. 구현 단계에서 실제 렌더링 결과를 확인하고, 필요하면 표시 전 정리(strip) 로직을 추가할지 결정해야 합니다.
- **레이트 리밋/캐시**: 매번 전체 파일을 다시 받아오면 API 호출이 늘어나므로 세션 단위 캐싱 권장.

---

## 8. 예상 저장소 구조

```
James0083/
├── README.md              (기존 프로필 README, 유지)
├── PLAN.md                 (본 계획서)
└── economic-briefing/       (새 웹앱 디렉터리)
    ├── index.html
    ├── style.css
    └── app.js
```

---

## 9. 다음 단계

구현을 진행하려면 아래 사항만 확정하면 됩니다:
1. **인증 방식**: 방법 A(OAuth 로그인, 권장) 로 진행할지 확인
2. **배포 위치**: 이 저장소(`james0083/james0083`)에 프로젝트 페이지로 배포할지, 별도 저장소를 새로 만들지
3. Google Cloud Console에서 OAuth 클라이언트 ID 발급은 사용자 본인의 구글 계정으로 진행해야 하는 작업이라, 이 부분은 제가 대신할 수 없고 안내에 따라 직접 진행해 주셔야 합니다.

위 사항이 확정되면 바로 구현에 착수할 수 있습니다.
